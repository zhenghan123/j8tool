   name: My Workflow

   on: [push, pull_request]

   jobs:
     myJob:
       runs-on: ubuntu-latest

       steps:
       - name: Check out code
         uses: actions/checkout@v2

       - name: Run a script
         run: |
           echo "Hello, world!"
